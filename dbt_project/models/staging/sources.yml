version: 2
sources:
  - name: raw
    schema: raw
    tables:
      - name: customers
        columns:
          - name: customer_id
            tests: [unique, not_null]
      - name: products
        columns:
          - name: product_id
            tests: [unique, not_null]
      - name: orders
        columns:
          - name: order_id
            tests: [unique, not_null]
      - name: order_items
        columns:
          - name: order_item_id
            tests: [unique, not_null]
      - name: payments
        columns:
          - name: payment_id
            tests: [unique, not_null]
