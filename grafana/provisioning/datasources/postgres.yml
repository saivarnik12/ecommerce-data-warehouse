apiVersion: 1
datasources:
  - name: Ecommerce Warehouse
    type: postgres
    access: proxy
    url: postgres:5432
    user: dw_user
    secureJsonData:
      password: dw_password
    jsonData:
      database: warehouse
      sslmode: disable
      postgresVersion: 16
      timescaledb: false
    isDefault: true
